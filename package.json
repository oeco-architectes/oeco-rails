{
  "private": true,
  "scripts": {
    "version": "node -e 'console.log(require(\"./package\").engines.node)'"
  },
  "dependencies": {
    "babel-preset-latest": "^6.16.0",
    "babelify": "^7.3.0",
    "browserify": "^14.5.0",
    "browserify-incremental": "^3.1.1",
    "document-ready-promise": "^3.0.1",
    "lodash.debounce": "^4.0.7"
  },
  "babel": {
    "presets": [
      "latest"
    ]
  },
  "engines": {
    "node": "6.*"
  }
}
